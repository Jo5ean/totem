<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>UCASAL - Consulta tus exámenes</title>
		<!-- Favicons -->
		<link rel="icon" href="./cropped-FavicoUcasal-32x32.png" sizes="32x32" />
		<link rel="icon" href="./cropped-FavicoUcasal-192x192.png" sizes="192x192" />
		<link rel="apple-touch-icon" href="./cropped-FavicoUcasal-180x180.png" />
		<meta name="msapplication-TileImage" content="./cropped-FavicoUcasal-270x270.png" />
		<!-- Meta tags -->
		<meta name="description" content="Consulta tus exámenes en UCASAL - Universidad Católica de Salta">
		<meta name="keywords" content="UCASAL, exámenes, consulta, universidad, educación">
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	@import "tailwindcss";
	
	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		height: 100%;
		font-family: system-ui, sans-serif;
	}

	/* Animaciones personalizadas */
	@keyframes fadeIn {
		from {
			opacity: 0;
		}
		to {
			opacity: 1;
		}
	}

	@keyframes slideDown {
		from {
			opacity: 0;
			transform: translateY(-30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes slideUp {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.animate-fade-in {
		animation: fadeIn 1s ease-out;
	}

	.animate-slide-down {
		animation: slideDown 0.8s ease-out 0.2s both;
	}

	.animate-slide-up {
		animation: slideUp 0.8s ease-out 0.4s both;
	}
</style>
